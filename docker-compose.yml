services:
  unifi2netbox:
    image: ghcr.io/patricklind/unifi2netbox:latest
    container_name: unifi2netbox
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    command: ["python", "main.py", "-v"]
    restart: unless-stopped
